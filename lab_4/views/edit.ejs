<!DOCTYPE html>
<html lang="uk">
  <head>
    <meta charset="UTF-8" />
    <title>Редагування викладача</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="bg-light">
    <div class="container mt-5">
      <h1 class="text-center mb-4">Редагування викладача</h1>

      <div class="card shadow-sm">
        <div class="card-body">
          <form method="POST" action="/edit/<%= teacher.id %>" class="row g-3">
            <div class="col-md-4">
              <label class="form-label">Прізвище</label>
              <input
                class="form-control"
                name="surname"
                value="<%= teacher.surname %>"
                required
              />
            </div>
            <div class="col-md-4">
              <label class="form-label">Посада</label>
              <input
                class="form-control"
                name="position"
                value="<%= teacher.position %>"
                required
              />
            </div>
            <div class="col-md-4">
              <label class="form-label">Ступінь</label>
              <input
                class="form-control"
                name="degree"
                value="<%= teacher.degree %>"
                required
              />
            </div>
            <div class="col-md-4">
              <label class="form-label">Аудиторія</label>
              <input
                class="form-control"
                name="room"
                value="<%= teacher.room %>"
                required
              />
            </div>
            <div class="col-12 text-end">
              <button type="submit" class="btn btn-success">Зберегти</button>
              <a href="/" class="btn btn-secondary">Назад</a>
            </div>
          </form>
        </div>
      </div>
    </div>
  </body>
</html>
